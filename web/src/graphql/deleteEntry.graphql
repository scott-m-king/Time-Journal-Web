mutation deleteEntry($idArray: [Int!]!) {
  deleteEntry(idArray: $idArray) {
    entries {
      id
      title
      notes
      duration
      date
      categoryId
    }
    categories {
      id
      description
      duration
    }
  }
}
