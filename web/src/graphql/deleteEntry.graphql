mutation deleteEntry($id: Int!) {
  deleteEntry(id: $id) {
    entries {
      id
      title
      notes
      duration
      date
      categoryId
    }
    categories {
      id
      description
      duration
    }
  }
}